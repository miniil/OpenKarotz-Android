<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <!-- Radio icon -->
    <ImageView
        android:id="@+id/imageRadio"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_radio"
        android:contentDescription="@string/radio_icon_desc"/>

    <!-- Radio name -->
    <TextView
        android:id="@+id/textRadioName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:textSize="16sp"
        android:text="Radio Name"/>

    <!-- Play button -->
    <ImageButton
        android:id="@+id/buttonPlayRadio"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@android:drawable/ic_media_play"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/radio_play_desc"/>

</LinearLayout>